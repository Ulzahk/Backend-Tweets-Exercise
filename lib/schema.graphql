type Tweet {
    statuses: [tweetDetails],
}

type tweetDetails{
    created_at: String,
    id_str: String,
    text: String
}

""" type tweetMetaData {
    count: Integer
} """

type Query {
    "Devuelve todos los Tweets"
    getTweets: [Tweet]
}